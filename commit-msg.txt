feat(reservation): add cancel-confirmed flow with refund tracking and audit trail

- Add RefundStatus enum (PENDING/PROCESSING/COMPLETED/FAILED/NOT_REQUESTED)
- Add refund fields to Reservation: refundAmount, refundRequestedAt, refundReason, etc.
- Add statusHistory array for full audit trail on every status change
- Add NotificationType: RESERVATION_ADMIN_CANCELLED, RESERVATION_REFUND_*, RESERVATION_DEPOSIT_PAID
- Add cancelConfirmedReservation() method: handles refund, releases inventory, releases table, notifies customer
- Fix confirmDeposit(): atomic table lock (moved from createFullBooking), idempotency check, fixes double-deduction bug
- Fix rejectReservation(): releases inventory on rejection
- Add POST /reservations/:id/cancel-confirmed endpoint
- Fix: Add refund fields + statusHistory to reservation schema
- Fix: Expand NotificationPayload with refundStatus, refundAmount, priority fields
